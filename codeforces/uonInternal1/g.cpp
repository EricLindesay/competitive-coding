#include <bits/stdc++.h>

typedef long long ll;
#define int ll

using namespace std;

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	ll n, c;
	cin >> n >> c;

	vector<int> ns(n);
	for (ll &a : ns) {
		cin >> a;
	}

	

    return 0;
}
