#include <iostream>

typedef long long ll;

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;

	while (t--) {
		int n, m;
		cin >> n >> m;
		string init;
		cin >> init;

		vector<pair<string, int>> medicines;  // affect, 
		for (int i=0; i<)
	}

    return 0;
}
